export type WorkItem = {
    id: string;
    date: string;
    role: string;
    company: string;
    rank: number;
};

export type ServiceItem = {
    id: string;
    title: string;
    desc: string;
    rank: number;
}